/**
 * @fileoverview Shared type definitions for the GenreBadge component.
 */

/**
 * Visual variant options accepted by the Badge primitive.
 * Re-declared here to keep the movies feature self-contained.
 */
export type BadgeVariant = 'default' | 'success' | 'warning' | 'danger' | 'info'
