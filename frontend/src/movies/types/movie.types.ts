/**
 * @fileoverview Local type aliases for the movies feature.
 * Re-exports the canonical DTO from the shared types module so that
 * feature-level imports remain self-contained.
 */
export type { MovieDto } from '../../types/api.types'
