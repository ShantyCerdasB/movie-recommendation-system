/**
 * @fileoverview Local type aliases for the users feature.
 * Re-exports the canonical DTOs from the shared types module so that
 * feature-level imports remain self-contained.
 */
export type { UserDto, RatingDto } from '../../types/api.types'
