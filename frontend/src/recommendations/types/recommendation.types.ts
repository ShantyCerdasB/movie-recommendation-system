/**
 * @fileoverview Local type re-exports for the recommendations feature.
 * @description Isolates the feature from direct dependency on the global types barrel.
 */
export type { RecommendationDto } from '../../types'
